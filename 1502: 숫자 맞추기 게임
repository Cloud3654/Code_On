#include <iostream>
using namespace std;
int main() {
    int n;
    while (true) {
        cin >> n;
        cout << (n > 4 ? "Down" : (n < 4 ? "Up" : "BINGO")) << endl;
        if (n == 4) break;
    }
}
